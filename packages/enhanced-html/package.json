{
  "name": "enhanced-html",
  "version": "0.5.0",
  "license": "MIT",
  "main": "./dist/eh.js",
  "files": [
    "dist",
    "src",
    "package.json",
    "README.md"
  ],
  "scripts": {
    "build": "tsc && vite build",
    "test": "tsc && vite build && vitest run"
  }
}